name: news_repository
description: Dart package which manages the news domain.
version: 1.0.0

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  equatable: ^2.0.3
  html_unescape: ^2.0.0

  webfeed: ^0.7.0
  http: ^0.13.4

dev_dependencies:
  flutter_test:
    sdk: flutter